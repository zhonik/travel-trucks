const CamperItem = ({ camper }) => {
  const {
    AC,
    TV,
    bathroom,
    consumption,
    description,
    engine,
    form,
    gallery,
    gas,
    height,
    id,
    kitchen,
    length,
    location,
    microwave,
    name,
    price,
    radio,
    rating,
    refrigerator,
    reviews,
    tank,
    transmission,
    water,
    width,
  } = camper;

  return <div>{name}</div>;
};

export default CamperItem;
